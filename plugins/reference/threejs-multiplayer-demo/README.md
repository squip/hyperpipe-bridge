# Three.js Multiplayer Demo (Reference Plugin)

Demonstrates:

- 3D route UI rendered by plugin runner HTML.
- Host-managed cross-instance signaling with WebRTC offer/answer/ICE.
- Real WebRTC data channel for multiplayer state sync.
- Plugin iframe bridge with `media-create-session`, `media-join-session`, and `media-send-signal`.
