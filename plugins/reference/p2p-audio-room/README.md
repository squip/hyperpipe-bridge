# P2P Audio Room (Reference Plugin)

Demonstrates:

- `media.session` + `p2p.session` command usage.
- Host-managed cross-instance signaling transport for SDP/ICE exchange.
- Real WebRTC audio/video track exchange between app instances.
- Plugin iframe bridge to host IPC-backed media commands.
